const $=(id)=>document.getElementById(id);function esc(s){return String(s||'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}function fmt(ms){ms=Math.max(0,ms||0);const s=Math.floor(ms/1000),m=Math.floor(s/60),sec=s%60,h=Math.floor(m/60);return h?`${h}:${String(m%60).padStart(2,'0')}:${String(sec).padStart(2,'0')}`:`${String(m).padStart(2,'0')}:${String(sec).padStart(2,'0')}`}function left(iso){return iso?new Date(iso).getTime()-Date.now():0}function phaseLabel(p){return({lobby:'Lobby',first_impressions:'First Impressions',couple_up:'Couple Up',mingle:'Mingle Time',redflag:'Red Flag / Green Flag',chemistry:'Chemistry Check',spill:'Spill the Tea',temptation:'Temptation / Switch-Up',recoupling:'Recoupling',survival:'Survival Challenge',audience_vote:'Audience Voting',finale_pitch:'Finale Pitch',final_winner_vote:'Final Winner Vote',winner:'Winner Reveal',cashout:'Cash Out or Double Down',replacement:'Replacement Teams',ended:'Ended'})[p]||p}function initials(n){return(n||'?').trim().split(/\s+/).slice(0,2).map(x=>x[0]?.toUpperCase()).join('')||'?'}function avatar(p){return p?.photo?`<img class="avatar big" src="${p.photo}">`:`<div class="avatar big">${initials(p?.name)}</div>`}function activeCouples(s){return(s?.couples||[]).filter(c=>c.status==='active')}function playersFor(s,c){return(c?.playerIds||[]).map(id=>(s.players||[]).find(p=>p.id===id)).filter(Boolean)}function coupleName(s,c){return playersFor(s,c).map(p=>p.name).join(' + ')||'Open Couple'}function gameClock(s){if(!s?.gameStartedAt)return'00:00';return fmt(Date.now()-new Date(s.gameStartedAt).getTime()-(s.gameClockPausedMs||0))}function counts(s){const key=s.currentVoteKey;const c={};(s.votes||[]).filter(v=>!key||v.voteKey===key).forEach(v=>c[v.targetCoupleId]=(c[v.targetCoupleId]||0)+1);return c}let data=null;async function load(){try{const r=await fetch('/api/sessions');data=await r.json();render()}catch(e){}}function cue(s){const m={lobby:'Scan to join Bar Island.',mingle:'Couples, start mingling.',audience_vote:'Vote now using your phone.',final_winner_vote:'Vote for the Bar Island Champions.',cashout:'Top teams decide: cash out or double down.',replacement:'Bombshell replacement teams may enter.',winner:'Bar Island Champions!'};return m[s.phase]||'Follow the host and watch the island change.'}function render(){const s=data?.activeSession;if(!s){$('pub').innerHTML=`<div class="public-wrap"><div class="public-center"><div><div class="phase-title">BAR ISLAND</div><p class="cue">Waiting for next game</p></div></div></div>`;return}const timer=s.phase==='lobby'?fmt(new Date(s.startTime).getTime()-Date.now()):fmt(left(s.phaseEndsAt));const vc=counts(s);let cards='';if(['audience_vote','final_winner_vote','winner','cashout','replacement'].includes(s.phase)){cards=activeCouples(s).slice(0,6).map(c=>`<div class="public-card"><div class="row" style="justify-content:center">${playersFor(s,c).map(p=>avatar(p)).join('')}</div><h3>${esc(coupleName(s,c))}</h3><div class="pill">${vc[c.id]||0} votes</div></div>`).join('')}else{cards=activeCouples(s).slice(0,6).map(c=>`<div class="public-card"><h3>${esc(coupleName(s,c))}</h3><div class="row" style="justify-content:center">${playersFor(s,c).map(p=>avatar(p)).join('')}</div></div>`).join('')}const winner=s.winnerCoupleId?s.couples.find(c=>c.id===s.winnerCoupleId):null;$('pub').innerHTML=`<div class="public-wrap"><div class="public-top"><div><b>BAR ISLAND</b><div class="muted">Code: ${esc(s.code)} • Block ${s.blockIndex}/${s.maxBlocks}</div></div><div class="game-clock">Game Clock: ${gameClock(s)}</div></div><div class="public-center"><div><div class="phase-title">${esc(phaseLabel(s.phase))}</div>${s.phaseEndsAt||s.phase==='lobby'?`<div class="timer public">${timer}</div>`:''}<div class="cue">${winner?`Winner: ${esc(coupleName(s,winner))}`:esc(cue(s))}</div></div></div><div class="public-grid">${cards}</div></div>`}setInterval(load,3000);setInterval(()=>{if(data)render()},1000);load();
